import React from 'react';
import './AnimationCss.css'; // Make sure to import the updated CSS

const HoverButton = ({ buttonText }) => {
  return (
    <div className="">
<button>
      {buttonText}
    </button>
    </div>
  );
};

export default HoverButton;
